# =============================================================================
# RAIDEN VISUAL PACKAGE - Environment Configuration
# =============================================================================
# Copy to .env in your project root:
#   cp src/visual/.env.example .env

# =============================================================================
# STREET VIEW / FACADE IMAGES (choose one)
# =============================================================================

# Google Street View (recommended - best coverage)
# Get key at: https://console.cloud.google.com/apis/credentials
# Enable: Maps Static API, Street View Static API
GOOGLE_API_KEY=your-google-api-key

# Mapillary (alternative - free, crowdsourced)
# Get token at: https://www.mapillary.com/developer
MAPILLARY_TOKEN=your-mapillary-token

# =============================================================================
# LLM ANALYSIS (optional but recommended)
# =============================================================================

# Gemini (recommended - fast, accurate, included with Google API key)
# Uses same GOOGLE_API_KEY as above

# Komilion (alternative - free tier, router to multiple LLMs)
# Contact: komilion.ai
KOMILION_API_KEY=sk-komilion-...

# Claude (alternative)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (alternative)
OPENAI_API_KEY=sk-...

# =============================================================================
# NO API KEY NEEDED FOR:
# =============================================================================
# - ESRI Satellite imagery (always free)
# - SAM segmentation (local model, downloads automatically)
# - OpenCV-based WWR detection (local)
# - Edge detection footprint extraction (local)
